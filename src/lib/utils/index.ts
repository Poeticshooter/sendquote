/**
 * Shared utility functions.
 *
 * This directory is reserved for generic helper functions that are
 * used across multiple modules. If a utility is specific to one
 * domain, co-locate it with that domain instead.
 */
